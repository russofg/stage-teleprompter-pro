{
  "scripts": {
    "desktop": "MACOSX_DEPLOYMENT_TARGET=11.0 CARGO_BUILD_JOBS=1 npm run tauri dev",
    "build-desktop": "MACOSX_DEPLOYMENT_TARGET=11.0 CARGO_BUILD_JOBS=1 npm run tauri build",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  }
}
